#include "../include/GameConfig.hpp"

namespace Config {
    std::string GameTitle = "R-Type";
    sf::Vector2f ButtonSize = sf::Vector2f(125, 125);
    sf::Vector2f SettingsGameButtonSize = sf::Vector2f(100, 100);
}
